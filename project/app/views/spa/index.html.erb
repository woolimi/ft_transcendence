<div id="app">
<% if !user_signed_in? %>
	<%= render "login" %>
<% else %>
	<%= render "navbar" %>

	<div class="container-fluid" id="content-wrapper">
		<div class="row h-100">
			<div class="col-md-8 border" id="view-content">
				<!-- view-content -->
			</div>
			<div class="col-md-4">
				<div class="row h-100">
					<%= render "channels" %>
					<%= render "friends" %>
				</div>
			</div>
		</div>
	</div>
	<%= render "userInfoModal" %>
	<%= render "searchUserModal" %>
	<%= render "searchBlockUserModal" %>
<% end %>
</div>
